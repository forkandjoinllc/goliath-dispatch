<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- MySQL, no SQLite. El esquema son 15 ficheros de DDL MySQL en crudo
             (columnas generadas STORED, CHECK, triggers con SIGNAL, claves
             foráneas compuestas). SQLite no ejecuta nada de eso, así que una
             suite sobre SQLite probaría un esquema que no es el que se despliega.
             Ver docs/mysql-port.md. -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="goliath_l_test"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <!-- database, no array: la empresa activa y el estado del segundo
             factor viven en COLUMNAS de `sessions` (active_tenant_id,
             mfa_satisfied_at, revoked_at). Con el driver array esas columnas no
             existen en ningún sitio y las pruebas que las comprueban no
             probarían nada. Ver docs/mysql-port.md, sección Sesiones. -->
        <env name="SESSION_DRIVER" value="database"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
